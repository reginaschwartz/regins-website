{
  "name": "regins-whatsapp-bot",
  "version": "1.0.0",
  "description": "WhatsApp Cloud API webhook that runs a branching intake conversation",
  "type": "module",
  "main": "src/server.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "debug": "node --inspect src/server.js",
    "simulate": "node src/simulate.js",
    "test": "node --test"
  },
  "dependencies": {
    "express": "^5.1.0"
  }
}
