# One app, one published port. Classic docker-compose 1.x: use format 3.3.
version: "3.3"

services:
  app:
    build: .
    image: regins-website:latest
    container_name: regins-website
    ports:
      - "9000:3000"
    env_file:
      - env.env
    restart: unless-stopped
