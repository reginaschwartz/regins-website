{
  "name": "regins-website",
  "version": "1.0.0",
  "description": "Personal site and WhatsApp Cloud API webhook on one port",
  "type": "module",
  "main": "server/server.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server/server.js",
    "dev": "node --watch server/server.js",
    "debug": "node --inspect server/server.js",
    "simulate": "node server/simulate.js",
    "test": "node --test"
  },
  "dependencies": {
    "express": "^5.1.0"
  }
}
